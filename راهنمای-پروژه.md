# راهنمای پروژه‌ی سامانه‌ی ثبت‌درخواست (registration-app-v1)

این سند برای مرجع بعدی نوشته شده تا هر زمان لازم شد، بدون نیاز به مرور کل تاریخچه‌ی گفتگو، بشه به پروژه ادامه داد.

---

## ۱. معرفی کلی

یک سامانه‌ی داخلی سازمانی برای ثبت درخواست کالا توسط کارمندان، با یک پنل مدیریت کامل. کارمند با وارد کردن **کد ملی** خودش، مشخصاتش (که مدیر از قبل از طریق فایل اکسل بارگذاری کرده) به‌صورت خودکار نمایش داده می‌شه، شماره تماس وارد می‌کنه، محصولات مورد نیازش رو انتخاب می‌کنه و درخواست ثبت می‌شه.

## ۲. معماری فنی

- **بک‌اند:** FastAPI (پایتون) + SQLAlchemy + PostgreSQL
- **فرانت‌اند:** React + TypeScript + TailwindCSS
- **اجرا:** یک ایمیج Docker واحد که هم بک‌اند رو اجرا می‌کنه هم فایل‌های build شده‌ی فرانت‌اند رو به‌صورت استاتیک سرو می‌کنه (`Dockerfile` چندمرحله‌ای)
- **دیتابیس:** به‌صورت یک سرویس جدا در `docker-compose.yml` تعریف شده
- **مهاجرت دیتابیس:** چون از Alembic استفاده نشده، تغییرات ستون‌ها با دستورات `ALTER TABLE ... IF NOT EXISTS` در ابتدای اجرای `main.py` به‌صورت خودکار و امن روی دیتابیس موجود اعمال می‌شن (بدون از دست رفتن داده)

### ساختار پوشه‌ها
```
backend/
  app/
    models.py        # مدل‌های دیتابیس (SQLAlchemy)
    schemas.py        # اسکیماهای ورودی/خروجی (Pydantic) — ورودی و خروجی هر موجودیت عمداً جدا تعریف شده
    crud.py            # توابع خواندن/نوشتن دیتابیس
    main.py            # نقطه‌ی ورود FastAPI + مهاجرت‌های خودکار دیتابیس
    routers/
      products.py            # محصولات
      requests.py            # درخواست‌های کاربران (بر پایه‌ی کدملی)
      admin.py                # لاگین ادمین + خروجی CSV/Excel + پاک‌سازی کامل
      settings.py             # مهلت ویرایش
      service_locations.py    # محل‌های خدمت (قدیمی، دیگر در فرم عمومی استفاده نمی‌شود)
      authorized_persons.py   # افراد مجاز (آپلود/خروجی اکسل، جست‌وجوی عمومی با کدملی)
frontend/
  src/pages/
    PublicForm.tsx      # فرم عمومی ثبت‌درخواست
    AdminLogin.tsx       # ورود ادمین
    AdminDashboard.tsx   # پنل مدیریت (تب‌بندی‌شده)
```

## ۳. مدل‌های اصلی دیتابیس

| موجودیت | فیلدهای کلیدی |
|---|---|
| `AuthorizedPerson` | کدملی (یکتا، کلید اصلی)، کدپرسنلی، نام، نام‌خانوادگی، محل خدمت (متن آزاد)، وضعیت خدمت (متن آزاد) |
| `Product` | نام، نوع، `price` (قیمت **به ازای هر واحد**، ریال)، `unit_name` (واحد، مثلاً کیلوگرم)، `package_size` (مقدار هر بسته به همان واحد)، `max_quantity` (حداکثر تعداد بسته)، `is_active` (فعال/غیرفعال) |
| `Request` | `national_code` (کدملی، شناسه‌ی اصلی)، نام کامل/کدپرسنلی/وضعیت خدمت/محل خدمت (همه در لحظه‌ی ثبت از `AuthorizedPerson` واکشی می‌شن، نه از ورودی کاربر)، `phone_number`، آیتم‌های سفارش |
| `ServiceLocation` | قدیمی — جدول و تب مدیریتش نگه داشته شده ولی فرم عمومی دیگه ازش استفاده نمی‌کنه |

## ۴. جریان فرم عمومی (کاربر)

1. کاربر کد ملی (دقیقاً ۱۰ رقم، فقط عدد) رو وارد و «جست‌وجو» می‌زنه.
2. اگر کدملی در لیست افراد مجاز نبود → پاپ‌آپ «خطا در ثبت»: *«کد ملی شما در سامانه ثبت نشده است، برای حل مشکل به امور مالی اداره مراجعه نمایید.»*
3. اگر پیدا شد و **قبلاً درخواستی نداشت** → مستقیم فرم باز می‌شه؛ مشخصات (کدپرسنلی، نام، محل خدمت، وضعیت خدمت) فقط‌خواندنی نمایش داده می‌شن.
4. اگر **قبلاً درخواست ثبت کرده** → قبل از باز شدن فرم، باید شماره تلفنی که دفعه‌ی قبل باهاش ثبت‌نام کرده رو دوباره وارد کنه (تأیید هویت، هم در فرانت و هم در بک‌اند چک می‌شه).
5. کاربر شماره تماس (فرمت موبایل ایران، الزامی) و تعداد بسته‌ی هر محصول رو وارد می‌کنه. دکمه‌ی «ثبت» تا وقتی حداقل یک محصول انتخاب نشده، غیرفعاله.
6. لیبل «مجموع کالاها» قبل از دکمه‌ی ثبت، جمع مبلغ رو نشون می‌ده: `تعداد بسته × مقدار هر بسته × قیمت واحد`.
7. بعد از ثبت/ویرایش موفق، یک پاپ‌آپ موفقیت نشون داده می‌شه و کاربر کاملاً از فرم خارج می‌شه (برمی‌گرده به مرحله‌ی اول).

**نکته‌ی امنیتی:** فیلدهای هویتی (نام، کدپرسنلی، محل خدمت، وضعیت خدمت) هیچ‌وقت مستقیم از کاربر پذیرفته نمی‌شن — همیشه سمت سرور از روی جدول افراد مجاز واکشی می‌شن تا با دستکاری درخواست HTTP هم قابل جعل نباشن.

## ۵. راهنمای پنل مدیریت

آدرس ورود: `/admin/login` — نام کاربری/رمز پیش‌فرض: `admin` / `admin123` (⚠️ حتماً بعد از اولین ورود این رمز رو عوض کنید یا حداقل به کسی ندید — چون در کد hardcode شده و پروژه روی گیت‌هاب عمومی/خصوصی قرار داره).

پنل به‌صورت تب‌بندی‌شده است:

### تب «درخواست‌ها»
لیست کامل درخواست‌های ثبت‌شده (کدملی، کدپرسنلی، نام، وضعیت خدمت، محل خدمت، تاریخ). امکانات: مشاهده‌ی جزئیات، افزودن توضیحات ادمین، فعال/غیرفعال کردن قابلیت ویرایش هر درخواست، حذف، و خروجی CSV/Excel (شامل مبلغ کل هر درخواست).

### تب «افزودن اسامی افراد»
- **آپلود اکسل:** ستون‌های الزامی: `کدملی`، `کدپرسنلی`، `نام`، `نام خانوادگی`، `محل خدمت`، `وضعیت خدمت`. اگر کدملی از قبل وجود داشته باشه، اطلاعاتش با مقادیر جدید **جایگزین** می‌شه (upsert). ردیف‌های ناقص نادیده گرفته می‌شن و گزارش خطا نشون داده می‌شه.
- **دانلود فایل نمونه:** دکمه‌ای برای گرفتن یک فایل خالی با فرمت درست.
- **افزودن دستی:** برای اضافه/ویرایش/حذف تک‌تک افراد.
- **خروجی CSV/Excel** از کل لیست.
- **پاک‌سازی کامل:** دکمه‌ای (با دو تأیید) که فقط «افراد مجاز» و «درخواست‌ها» رو پاک می‌کنه (محصولات و تنظیمات دست‌نخورده می‌مونن) — برای شروع مجدد با یک فایل اکسل تازه.

### تب «محصولات»
هر محصول: نام، نوع، حداکثر تعداد بسته، **واحد** (مثلاً کیلوگرم/عدد/لیتر)، **مقدار هر بسته** (مثلاً ۲ برای بسته‌ی ۲کیلویی)، **قیمت به ازای هر واحد** (نه هر بسته!)، توضیحات، و دکمه‌ی **فعال/غیرفعال** (محصول غیرفعال از فرم عمومی حذف می‌شه ولی داده‌اش باقی می‌مونه، قابل فعال‌سازی مجدد).

> مثال: «گوشت قرمز» با بسته‌بندی ۲ کیلوگرمی → واحد = «کیلوگرم»، مقدار هر بسته = ۲، قیمت = قیمتِ هر کیلوگرم (نه قیمت کل بسته).

### تب «محل‌های خدمت»
باقی‌مانده از نسخه‌ی قدیمی؛ فرم عمومی دیگه ازش استفاده نمی‌کنه (چون محل خدمت حالا از فایل اکسل افراد مجاز میاد).

### تب «تنظیمات»
تعیین آخرین مهلت ویرایش درخواست‌ها.

## ۶. دیپلوی

### تاریخچه
- در ابتدا روی **Render** دیپلوی شده بود.
- به دلیل الگوی مصرف محدود (~۱۰ روز در ماه) و نیاز به سرور ایرانی، پروژه به یک **ریپازیتوری کاملاً جدید و مستقل** به اسم `registration-app-v1` منتقل شد (تاریخچه‌ی گیت جدا، بدون ارتباط با ریپوی قبلی).
- در حال مهاجرت به یک VPS ایرانی (ArvanCloud، پلن `eco-small2`: ۱ CPU / ۲GB رم) با Docker Compose.

### وضعیت فعلی مهاجرت (آخرین نقطه‌ی متوقف‌شده)
روی سرور: Docker نصب شده، پروژه clone شده در `/opt/registration-app-v1`. مرحله‌ی بعدی: اصلاح `docker-compose.yml` قبل از اولین اجرا:
1. جایگزینی `SECRET_KEY` با یک مقدار امن تصادفی
2. تغییر رمز پیش‌فرض دیتابیس (`admin`/`123456`)
3. **حذف mapping پورت ۵۴۳۲** از سرویس `db` (در حال حاضر پستگرس مستقیم به اینترنت باز است — این باید بسته بشه چون کاربر داخلی همون شبکه‌ی Docker به دیتابیس دسترسی داره و نیازی به expose عمومی نیست)

سپس: `docker compose up -d --build`، تنظیم DNS دامنه، نصب Nginx + Certbot برای HTTPS، و بستن فایروال (`ufw`) به‌جز پورت‌های ۲۲/۸۰/۴۴۳.

### آپدیت کد بعد از هر تغییر
```bash
cd ~/registration-app-v1   # یا مسیر روی سرور: /opt/registration-app-v1
git add .
git commit -m "توضیح تغییر"
git push origin main
```
اگر روی Render باشه، دیپلوی خودکاره. روی VPS خودمدیریت‌شده، باید دستی `git pull` و `docker compose up -d --build` رو روی سرور اجرا کنید (یا یک اسکریپت/وب‌هوک ساده براش تنظیم کنیم).

## ۷. نکات مهم قبل از استفاده‌ی واقعی

- بعد از هر بار آپلود لیست افراد مجاز، حتماً محصولات (به‌خصوص قیمت و بسته‌بندی) رو بررسی کنید.
- محصولاتی که موجود نیستن رو **غیرفعال** کنید، نه حذف (برای حفظ تاریخچه و امکان برگشت سریع).
- رمز عبور پیش‌فرض ادمین رو تغییر بدید یا محدودش کنید.
- از دیتابیس، به‌خصوص قبل از هر «پاک‌سازی کامل»، بک‌آپ بگیرید.

---
*این فایل به‌صورت خودکار توسط دستیار هوش مصنوعی و بر اساس تاریخچه‌ی توسعه‌ی پروژه تهیه شده است.*
